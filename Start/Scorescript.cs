using System.Collections;
using UnityEngine;
using UnityEngine.UI; // Add this line for Unity UI
using TMPro;

public class Score : MonoBehaviour
{
    public Leaderboard leaderboard; // Référence à l'objet Leaderboard
    public InputField scoreInputField; // Référence au Unity UI InputField
    private int score;

    // Start is called before the first frame update
    
}
